# Django Docker Alpine Project 🐳

This project demonstrates containerizing a Django application using a lightweight Alpine Linux Docker image.

## 🔧 Tech Stack
- Django
- Python 3
- Docker
- Alpine Linux
- Gunicorn

## 🚀 How to Run Locally

docker build -t django-alpine-app .
docker run -p 8000:8000 django-alpine-app

